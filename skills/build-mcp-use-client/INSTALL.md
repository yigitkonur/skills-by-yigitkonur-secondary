# build-mcp-use-client

Building or auditing MCP client apps with mcp-use TypeScript, including MCPClient, sessions, React hooks, code mode, auth, CLI.

**Category:** development

## Install

Install this skill individually:

```bash
npx -y skills add -y -g yigitkonur/skills-by-yigitkonur-secondary/skills/build-mcp-use-client
```

Or install the full pack:

```bash
npx -y skills add -y -g yigitkonur/skills-by-yigitkonur-secondary
```

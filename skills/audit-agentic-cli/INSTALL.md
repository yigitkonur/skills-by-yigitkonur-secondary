# audit-agentic-cli

auditing a CLI for agent-readiness, designing agent-first command contracts, or adding iterative feedback loops for repairable machine-generated artifacts.

**Category:** development

## Install

Install this skill individually:

```bash
npx -y skills add -y -g yigitkonur/skills-by-yigitkonur-secondary/skills/audit-agentic-cli
```

Or install the full pack:

```bash
npx -y skills add -y -g yigitkonur/skills-by-yigitkonur-secondary
```

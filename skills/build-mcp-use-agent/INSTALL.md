# build-mcp-use-agent

Building TypeScript mcp-use MCPAgent code where an LLM chooses and orchestrates MCP tools.

**Category:** development

## Install

Install this skill individually:

```bash
npx -y skills add -y -g yigitkonur/skills-by-yigitkonur-secondary/skills/build-mcp-use-agent
```

Or install the full pack:

```bash
npx -y skills add -y -g yigitkonur/skills-by-yigitkonur-secondary
```
